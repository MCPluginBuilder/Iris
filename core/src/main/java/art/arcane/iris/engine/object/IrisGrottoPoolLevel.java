package art.arcane.iris.engine.object;

import art.arcane.iris.engine.object.annotations.Desc;

@Desc("Where the pool at the bottom of a coastal grotto sits.")
public enum IrisGrottoPoolLevel {
    @Desc("The pool surface sits at the dimension fluid height, so the grotto meets the sea.")
    SEA_LEVEL
}
